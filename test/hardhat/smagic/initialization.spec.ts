import { expect } from '../../setup'

describe('[hardhat] smock: initialization tests', () => {
  describe('initialization: other', () => {
    describe('from a contract name', () => {
      it('should be able to create a SmockContract from a contract name', async () => {
        
      })
  
      it('should throw if the corresponding JSON contract ABI does not include the necessary fields', async () => {

      })
    })

    describe('from a JSON contract ABI', () => {
      it('should be able to create a SmockContract from a JSON contract ABI', async () => {

      })
  
      it('should throw if the ABI does not include the necessary fields', async () => {

      })
    })
  })
})
